const config = {
  baseUrl: process.env.REACT_APP_BASE_URL || 'http://localhost:8080',
  // network timeout in miliseconds
  networkTimeout: 5000,
  theme: "mui", // "cui" (core ui)
                // "mui" (material ui)
  //

}

export default config;
